
<% if current_user.is_followed(user_id) %>
  <%= link_to "Unfollow", follow_path(user_id), :method => :delete, remote: true %>
<% else %>
  <%= link_to "Follow", follows_path(:user_id => user_id), :method => :post, remote: true %>
<% end %>
